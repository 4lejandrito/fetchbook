{
  "name": "fetchbook",
  "version": "0.0.15",
  "description": "Manage your HTTP requests",
  "author": "Alejandro Tardín <alejandro@tardin.com>",
  "license": "MIT",
  "dependencies": {
    "bun": "^1.0.3"
  },
  "devDependencies": {
    "json-server": "^0.17.4",
    "start-server-and-test": "^2.0.1"
  },
  "bin": {
    "fetchbook": "./bin/fetchbook.js"
  },
  "scripts": {
    "test": "start-test test:start 3000 test:run",
    "test:run": "npx fetchbook test/story.ts -v",
    "test:start": "json-server --quiet --host :: test/db.json"
  }
}
